package SnakePj.main;

import javafx.stage.Stage;

public interface ManagerGame {
    public void startGame(Stage primaryStage);
    public void restart(Stage primaryStage);
    public void exit(Stage primaryStage);
}
